# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(MS_Sign_LUT)

ADD_COMPONENT(MS_Barcode_Macro)

ADD_COMPONENT(MS_Sign_Decide)

ADD_COMPONENT(MS_Barcode_Decide)

ADD_COMPONENT(MS_Barcode_Prepare)

ADD_COMPONENT(MS_Blueball_LUT)

ADD_COMPONENT(MS_Blueball_Decide)
